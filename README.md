Created by David Cooper (davidcooper@vt.edu)

To run the project, download the src folder and install Node.js on your machine.

Run "> node server.js" to start the server.

JNavigate to http://localhost:3000/upload.html to upload self-contained HTML files.

Navigate to http://localhost:3000/pages.html to access those files.
